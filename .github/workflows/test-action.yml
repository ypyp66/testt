# name: test-action

# on:
#   push:
#     branches: [main]
#   pull_request:
#     branches: [main]

# jobs:
#   build:
#runs-on과 strategy를 통해 워크플로우 환경 설정하기!
#ubuntu 환경설정 / max일때는 runs-on: macos-latest
# runs-on: ubuntu-latest

# strategy:
#   matrix:
#     node-version: [14.x, 16.x]

#브랜치 체크아웃
#name은 각 스텝항목의 이름, uses로 사용할 액션을 지정 => main 브랜치 체크아웃하기
# steps:
#   - uses: actions/checkout@v2
#   - name: Use Node.js ${{ matrix.node-version }}
#     uses: actions/setup-node@v2
#     with:
#       node-version: ${{ matrix.node-version }}
#       cache: "npm"
#   - name: Build
#     run: yarn run build
#     env:
#       PUBLISH_BRANCH: main
#       PUBLISH_DIR: ./build
#       SCRIPT_MODE: true
# - run: npm ci
# - run: npm run build --if-present
# - run: npm test

name: test-action

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Setup Node
        uses: actions/setup-node@v2.1.2
        with:
          node-version: "16.x"

      - name: Cache dependencies
        uses: actions/cache@v2
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - run: npm ci
      - run: npm run build

      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./build
